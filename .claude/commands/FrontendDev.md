---
description: FrontendDev skillini çalıştır — benzersiz, AI görünümü olmayan, SEO'su tam frontend tasarım/build
---

Read `SKILL.md` at this repository's root and follow its 6-phase workflow completely (brief intake → reference analysis → Design DNA → SEO foundation → build → screenshot self-critique). Load the referenced files under `references/` and `data/` as the workflow's routing table directs.

User arguments (brief, reference URLs, or both): $ARGUMENTS

If no arguments were given, start phase 1 by asking the intake questions.
